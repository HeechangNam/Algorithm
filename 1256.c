#include <stdio.h>
#include <string.h>

int* sort;
char buffer[405];
char mat[405][405];

int main() {

	int test_case;
	int t;

	int k;
	int buf_size;

	scanf("%d", &test_case);

	for (t = 0; t < test_case; t++) {
		scanf("%d", &k);
		scanf("%s", buffer);
		buf_size = strlen(buffer);
	}

	return 0;
}
